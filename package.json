{
  "name": "3-found",
  "version": "0.1.0",
  "private": true,
  "description": "自用 macOS 桌面应用",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "npm run check -w @found/frontend && npm run check -w @found/desktop"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "@types/node": "^24.0.10",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
