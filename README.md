# 3.found

一个自用桌面应用项目，采用前后端分离结构，并通过 Electron 打包为 macOS 应用。

## 目录

- `apps/backend`：本地后端 API
- `apps/frontend`：前端界面
- `apps/desktop`：macOS 桌面壳
- `docs`：项目文档
- `scripts`：项目脚本

## 常用命令

```bash
npm install
npm run dev
npm run build
npm run package:mac
```

